```Markdown
请生成一份完整单文件HTML网页，融合【单词展示页面】+【雅思单词听写练习工具】，全部逻辑写在同一个html文件，CDN引入第三方库，不需要后端服务。

## 页面顶部总结块（浅黄色背景，左侧黄色边框，仅保留UI容器，**删除内置示例单词文本**）
> 仅保留DOM结构，页面内不预置任何单词内容，单词数据源统一放在JS的wordListRaw数组，后续用户自行修改数组填入单词。

### 单词展示区交互要求
1. 禁止浏览器TTS语音合成，**使用谷歌词典牛津真人美音mp3直链：https://ssl.gstatic.com/dictionary/static/sounds/oxford/{单词}--_us_1.mp3**
2. 点击单词整行播放对应真人mp3音频；点击新单词自动停止上一条音频；audio标签隐藏。
3. 页面显著提示：本页面不能双击html本地打开(file协议跨域)，必须启动本地http服务访问。
4. UI样式：电脑手机自适应简洁干净；单词条目hover浅蓝色背景，鼠标pointer；英文单词蓝色加粗；tip提示红色小字；整体白色卡片容器+柔和阴影。

---
# 听写练习模块硬性交互规则（强制，全部保留）
1. 单词乱序开始练习，每一题自动播放美式发音；支持按钮重听当前单词。
2. 输入框输入单词，按下回车键提交答案判分。
3. 输入错误时：不要自动跳转下一题！页面必须显示【用户错误输入内容 + 标准答案 + 考点提示】，信息停留在页面，**必须用户再次按下回车键，才切换到下一题并播放读音**；答对之后同样需要回车进入下一题，不能自动切换。
4. 实时统计：进度(当前/总题数)、正确数量、错误数量、正确率。
5. 全部题目完成后：页面展示完整练习list清单，绿色标记答对单词，红色标记错误单词；错题项展示用户当时错误输入的内容。
6. 导出功能：练习结束后启用导出按钮，导出为.xlsx Excel文件。
Excel表格字段：序号、标准答案单词、中文释义、是否正确、用户输入、考点提示；表格末尾追加汇总行（总题、正确、错误、正确率），自动设置合适列宽，WPS/Excel可直接打开。
7. 重置按钮：点击后重新打乱单词顺序，清空所有答题记录、统计数据、结果面板，重新开始练习。
8. 增加“显示标准答案”按钮，随时查看当前单词释义和提示。
9. 页面底部完整单词库列表，渲染来自JS `wordListRaw`数组，点击任意单词可以播放发音。
10. 输入框禁止自动填充，练习结束输入框禁用；整体UI简洁清爽，电脑移动端自适应。

## JS单词数组约定
JS中变量 `wordListRaw` 保留数组结构，**初始为空数组 []**。
数组每一项格式：
`{"w":"单词","phonetic":"音标","zh":"中文释义","tip":"考试提示"}`
> 用户后续自行往数组里面追加单词。

## 技术约束
1. Excel导出使用SheetJS(xlsx) CDN，不要本地文件。
2. 发音统一调用谷歌词典美式mp3音频接口，禁止TTS。
3. 交互以键盘回车为主，按钮为辅。
4. 全部CSS样式内嵌HTML，单文件，外部仅CDN网络依赖。
5. 页面增加操作说明提示块。

输出完整全部HTML代码，不要省略、不要片段，可直接保存使用。
```